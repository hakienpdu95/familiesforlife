<?php

// Chưa có API route nào ở Phase 1 — public submission (Phase 2) sẽ dùng route web thường
// (form POST truyền thống), không cần API riêng. Giữ file rỗng để RouteServiceProvider
// (module_path($this->name, '/routes/api.php')) không lỗi khi boot.
